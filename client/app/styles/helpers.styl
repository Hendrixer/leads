makeShadow(level)
  if level is 1
    box-shadow 0 1px 3px alpha(black, .12), 0 1px 2px alpha(black, .24)
  else if level is 2
    box-shadow 0 3px 6px alpha(black, .16), 0 3px 6px alpha(black, .23);
  else if level is 3
    box-shadow 0 10px 20px alpha(black, .19), 0 6px 6px alpha(black, .23);
  else if level is 4
    box-shadow 0 14px 28px alpha(black, .25), 0 10px 10px alpha(black, .22)
